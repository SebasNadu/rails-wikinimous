<div class="container w-50 p-4">
  <div class="">
    <% @articles.each do |article| %>
    <div class="p-2">
      <article>
        <h2 class="mb-1"><%= link_to article.title, article %></h2>
        <p class="mb-1"><%= article.content[0..100] %></p>
      </article>
    </div>
    <% end %>
  </div>
</div>
